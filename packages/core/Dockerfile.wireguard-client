FROM linuxserver/wireguard:latest

# Install additional utilities without using cache to keep image size down
RUN apt-get update && apt-get install -y \
    nano \
    iputils-ping \
    traceroute \
    net-tools \
    curl \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*